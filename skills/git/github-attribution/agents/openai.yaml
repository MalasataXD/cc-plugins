interface:
  display_name: "GitHub attribution"
  short_description: "The attribution note that opens every write to GitHub. Use before running any `gh` command that creates or edits text on GitHub, or when another skill needs the note."
