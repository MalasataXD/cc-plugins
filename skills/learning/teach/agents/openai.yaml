interface:
  display_name: "Teach"
  short_description: "Multi-session tutor for any topic — invoke from your dedicated learning workspace; mission, lessons and learning records accumulate there."
policy:
  allow_implicit_invocation: false
